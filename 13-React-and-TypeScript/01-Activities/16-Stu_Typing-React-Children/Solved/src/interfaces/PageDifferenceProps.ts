export default interface PageDifferenceProps {
  // Since the children property of this component is a ReactNode, it can be any type that can be passed as children in JSX.
  children: React.ReactNode;
}
